# Copilot prompt:
# "Stub risk manager: functions to check max_loss_per_block and max_total_drawdown. Minimal implementation for extension."
def check_block_risk(block, settings):
    # return True if OK, False if risk limit exceeded
    # Placeholder: always OK in template
    return True

def check_global_risk(all_blocks, settings):
    return True
